Run `./main.sh` to generate a static site from markdown contents found in `content/`
Run `./test.sh` to run unit tests

**Lessons learned**

- Unit tests are great and I should write them more often
- AI can be a useful tool to generate test cases for edge cases
